import Button from "@/components/button/button";

const Home = () => {
  return (
    <div className="h-screen">
      <Button title="Sign in" variant={"basic"}/>
    </div>
  );
}

export default Home;
